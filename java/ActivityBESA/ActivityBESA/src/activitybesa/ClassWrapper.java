/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package activitybesa;
import activitybesa.classdata.ResultsClass;
import java.util.List;

/**
 *
 * @author mauricio
 */
public class ClassWrapper {
    /*
    static {
        // Code executes when created class
        System.loadLibrary("ActionDetection");
    } 
    
    public native void InitOpenPose();
    public native ResultsClass[] ProcessImage(byte[] image);
    public native String LoadVideo(String videoFile); // Returns UUID
    public native byte[] GetNextImage(String guid); // Returns ByteArray
    
    // JSON method
    public native String Convert2DPoint(String args);
    public native String CalibrateCamera(String args);
    public native String GetHomography(String args);
    public native String ProjectHomography(String args);
    */
}
